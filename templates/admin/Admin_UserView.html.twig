{% extends 'base.html.twig' %}

{% block title %} Support Cash Converters - Admin UserView {% endblock %}


{% block body %}

    <div class="card card_ticket">
        <div class="card-header">
            <h3> <i class="fa fa-pencil"> Modification Compte </i>
                {% if user.Profil.id == 1 %}
                     Utilisateur <span class="badge badge-pill badge-light">{{ user.login }} </span>
                {% else %}
                     Administrateur <span class="badge badge-pill badge-light">{{ user.login }} </span>
            {% endif %}
            </h3>

        </div>
        <div class="card-body">
            <div class="col-md-12 offset-md-12">
                <a href="{{ path('admin.user.all') }}" class="btn btn-outline-dark" role="button"><i class="fas fa-undo"> Retour à la liste d'Utilisateur </i> </a>
            </div>
            <br>

            {{ form_start(form) }}
                {{ form_row(form.magasin,{'attr': {'style': 'columns:12rem!important;'}} ) }}
            {#{{ form_widget(form) }}#}
            {<button type="submit" role="button" class="btn btn-outline-success"> <i class="fa fa-pencil"> Modifier </i></button> 

            {{form_end(form)}}

        </div>
    </div>
    <br>
{% endblock %}
