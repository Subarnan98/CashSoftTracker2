{% extends 'base.html.twig' %}

{% block title %} Support Cash Converters - Admin MagasinView {% endblock %}

{% block body %}
    <div class="card card_ticket">
        <div class="card-header">
            <h3> <i class="fa fa-pencil"> Modification Magasin :<span class="badge badge-pill badge-light">{{ magasin.nom}}</span>  </i></h3>

        </div>

        <div class="card-body">
            <a href="{{ path('admin.mag.all') }}" class="btn btn-outline-dark"><i class="fas fa-undo"> Retour à la liste des Magasins</i></a>
            <br>

            {{ form_start(form) }}

            {{ form_row(form) }}

                <button type="submit" role="button" class="btn btn-outline-success"> <i class="fa fa-pencil"> Modifier </i></button>
                {{form_end(form)}}
            </form>
        </div>
    </div>

{% endblock %}